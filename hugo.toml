baseURL = 'https://quint-theme-demo.netlify.app/'
languageCode = 'en-us'
title = '조남현 병원'
theme = "quint"
summaryLength = 20                  # Number of characters in your card summaries

[[menus.main]]
name = 'GitHub'
url = 'https://github.com/victoriadrake/hugo-theme-quint'

[markup.goldmark.renderer]
    unsafe = true
